import { ByteConvertorPipe } from './byte-convertor.pipe';

describe('ByteConvertorPipe', () => {
  it('create an instance', () => {
    const pipe = new ByteConvertorPipe();
    expect(pipe).toBeTruthy();
  });
});
